{
  "name": "discogs-uploader",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=backend",
    "dev:frontend": "npm run start --workspace=web",
    "dev:backend": "npm run dev --workspace=backend"
  }
} 